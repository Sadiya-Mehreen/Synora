# Synora -- Frontend

React + TypeScript + Vite frontend for Synora.

See the [project root README](../README.md) for what Synora is, architecture, setup instructions, and environment variables.

## Quick start

```cmd
npm install
npm run dev
```

Requires the backend running at `http://localhost:3000` (see `../backend`).
